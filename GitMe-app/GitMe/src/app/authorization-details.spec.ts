import { AuthorizationDetails } from './authorization-details';

describe('AuthorizationDetails', () => {
  it('should create an instance', () => {
    expect(new AuthorizationDetails()).toBeTruthy();
  });
});
